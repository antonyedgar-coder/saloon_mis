services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: saloon
      POSTGRES_PASSWORD: saloon
      POSTGRES_DB: saloon_mis
    ports:
      - "5432:5432"
    volumes:
      - saloon_mis_pg:/var/lib/postgresql/data

volumes:
  saloon_mis_pg:
